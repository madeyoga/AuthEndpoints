name: Docs

on:
  push:
    branches: [main]
    paths:
      - 'Documentation/**'
      - '.github/workflows/docs.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: Documentation
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install pnpm
        uses: pnpm/action-setup@v6
        with:
          # defaults.run.working-directory does not apply to uses: steps;
          # packageManager is in Documentation/package.json, not repo root
          package_json_file: Documentation/package.json
          version: 11.13.1

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: Documentation/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install

      - name: Generate static site
        env:
          NUXT_APP_BASE_URL: /AuthEndpoints/
        run: pnpm generate

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: Documentation/.output/public

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
