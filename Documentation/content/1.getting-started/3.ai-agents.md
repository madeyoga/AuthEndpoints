---
title: AI agents
description: Add the AuthEndpoints skill for coding agents.
navigation:
  icon: i-lucide-bot
---

Add the skill to your app.

```bash
npx skills add madeyoga/AuthEndpoints
```

The command copies `skills/authendpoints/` into `.agents/skills` or `.cursor/skills`.
